'use strict';

/**
 * tage-news-collection router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::tage-news-collection.tage-news-collection');
