'use strict';

/**
 * tage-news-collection service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::tage-news-collection.tage-news-collection');
