'use strict';

/**
 * coins-currency-type controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::coins-currency-type.coins-currency-type');
