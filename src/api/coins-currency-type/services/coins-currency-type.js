'use strict';

/**
 * coins-currency-type service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::coins-currency-type.coins-currency-type');
