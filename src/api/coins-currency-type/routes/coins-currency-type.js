'use strict';

/**
 * coins-currency-type router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::coins-currency-type.coins-currency-type');
