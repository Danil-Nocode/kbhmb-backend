'use strict';

/**
 * transfer-card-to-card-page service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::transfer-card-to-card-page.transfer-card-to-card-page');
