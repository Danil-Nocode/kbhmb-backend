'use strict';

/**
 * transfer-card-to-card-page router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::transfer-card-to-card-page.transfer-card-to-card-page');
